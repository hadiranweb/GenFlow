# =============================================================================
# GenFlow v2 — CI Pipeline
# =============================================================================

name: CI

on:
  push:
    branches:
      - main-platform
      - v2-island-architecture
  pull_request:
    branches:
      - main-platform

# Same as original — write-all is needed for GHCR + status checks
permissions: write-all

env:
  CARGO_TERM_COLOR: always

jobs:
  # ============================================================
  # Rust — format, clippy, unit tests
  # ============================================================
  rust:
    name: Rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Install Rust 1.88
        run: |
          rustup toolchain install 1.88 --component clippy,rustfmt --no-self-update 2>&1
          rustup default 1.88
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - run: cargo test --workspace --lib --locked --exclude genflow-gateway
      - run: cargo check -p genflow-gateway --locked

  # ============================================================
  # Docker — Build & push API image to GHCR
  # ============================================================
  docker-api:
    name: Docker API
    needs: [rust]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Generate image tags
        id: meta
        run: |
          echo "sha_short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
          echo "timestamp=$(date -u +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.DOCKER_PAT || secrets.GITHUB_TOKEN }}
      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/api:latest
            ghcr.io/${{ github.repository }}/api:${{ steps.meta.outputs.sha_short }}
            ghcr.io/${{ github.repository }}/api:${{ steps.meta.outputs.timestamp }}

  # ============================================================
  # Docker — Build & push Web image to GHCR
  # ============================================================
  docker-web:
    name: Docker Web
    needs: [rust]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Generate image tags
        id: meta
        run: |
          echo "sha_short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
          echo "timestamp=$(date -u +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.DOCKER_PAT || secrets.GITHUB_TOKEN }}
      - name: Build and push Web image
        uses: docker/build-push-action@v5
        with:
          context: ./apps/web
          file: apps/web/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/web:latest
            ghcr.io/${{ github.repository }}/web:${{ steps.meta.outputs.sha_short }}
            ghcr.io/${{ github.repository }}/web:${{ steps.meta.outputs.timestamp }}
